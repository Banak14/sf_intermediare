{% extends "front/base.html.twig" %}

{% block body %}

    {% for image in brand.media %}
		<img src="{{ asset('media/' ~ image.src) }}" alt="{{ image.alt }}">
	{% endfor %}
    <h2>{{brand.name}}</h2>
    <p>{{ brand.description}}</p>
    


{% endblock %}